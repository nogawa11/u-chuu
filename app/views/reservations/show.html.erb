<div class="reservation bg-black" style="padding-top: 88px">
  <div class="container p-5">
    <h1 class="main-heading mt-10 white">Reservation Details</h1>
    <div>
      <div class="row">
        <div class="col-12 col-lg-6">
          <div class="details white">
            <p><strong>Dates: </strong></p>
            <p><%= @reservation.start_date %> - <%= @reservation.end_date %></p>
            <p><strong>Guests: </strong></p>
            <p><%= @reservation.number_of_guests %> guests</p>
          </div>
        </div>
        <div class="col-12 col-lg-6">
          <div class="card-reservation">
            <div class="card-details m-5">
              <div class="card-product">
                <% if @reservation.planet.photos.first.present? %>
                  <%= cl_image_tag(@reservation.planet.photos.first.key) %>
                <% else %>
                  <img src="https://images.unsplash.com/photo-1464802686167-b939a6910659?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxzZWFyY2h8NXx8c3BhY2V8ZW58MHx8MHx8&auto=format&fit=crop&w=500&q=60" alt="planet" >
                <% end %>
                <div class="card-product-infos">
                  <div class="card-reservation-infos">
                    <h2><%= @reservation.planet.name %></h2>
                    <p class="mb-1"><%= @reservation.planet.description.split(" ").first(10).join(" ") %>...</p>
                    <span><%= @reviews_avg %><i class="fas fa-star warning"></i> - </span>
                    <span><%= @reservation.planet.reviews.count %> review(s)</span>
                  </div>
                </div>
              </div>
              <div class="price-details mt-4 pt-4 border-top">
                <h1>Price details</h1>
              </div>
              <div class="d-flex align-items-center justify-content-between">
                <span><%= number_to_currency(@reservation.planet.price_per_night) %> x <%= (@reservation.end_date - @reservation.start_date).to_i %> nights</span>
                <%= number_to_currency(@reservation.planet.price_per_night * (@reservation.end_date - @reservation.start_date).to_i) %>
              </div>
              <div class="d-flex align-items-center justify-content-between">
                <span>Service fee</span><%= number_to_currency(999) %>
              </div>
              <div class="d-flex align-items-center justify-content-between border-top mt-2 pt-2">
                <span><strong>Total</strong></span>
                <%= number_to_currency((@reservation.planet.price_per_night * (@reservation.end_date - @reservation.start_date).to_i) + 999) %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
