<h1>My Planets</h1>
<ul>
  <% @planets.each do |planet| %>
    <li>
      <%= link_to planet.name, planet_path(planet)%>
      <%= link_to "edit", edit_planet_path(planet) %>
      <%= link_to "delete", planet_path(planet), method: :delete, data: { confirm: "Are you sure?"}  %>
    </li>

  <% end %>
</ul>
